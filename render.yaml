services:
  - type: web
    name: chat-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server_cloud:app --host=0.0.0.0 --port=$PORT
    plan: free
    autoDeploy: true
    healthCheckPath: /health
